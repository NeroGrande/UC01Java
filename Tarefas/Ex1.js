// 1. Construir uma página na qual o usuário digitará dois números
// (através de prompt) e o programa abrirá um alert com a soma

let num = parseInt(prompt("Digite um número "));

let valor = parseInt(prompt("Digite outro número "));

let soma = num+valor;

alert ("A soma será:" +soma );