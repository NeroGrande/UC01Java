/*
Questão 16: Crie uma variável contador e atribua o valor 0.
Use um laço while para exibir no console os números de 0 a 9.
*/

let contador = 0; // Inicializa a variável contador com o valor 0

while (contador < 10) { // Enquanto o contador for menor que 10
  console.log(contador); // Exibe o valor de contador no console
  contador++; // Incrementa o valor de contador em 1
}

// Fim do programalet contador = 0; // Inicializa a variável contador com o valor 0

while (contador < 10) { // Enquanto o contador for menor que 10
  console.log(contador); // Exibe o valor de contador no console
  contador++; // Incrementa o valor de contador em 1
}
