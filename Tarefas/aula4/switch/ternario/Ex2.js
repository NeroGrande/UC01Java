/*
8. Peça ao usuário para digitar um número. Verifique se o número é positivo ou negativo. Caso seja positivo, exiba "Número Positivo", 
caso contrário, exiba "Número Negativo". Considere que o número zero não é nem positivo nem negativo.
*/

let num = parseInt(prompt("Digite um Número:"))
let resultado = num > 0 ? "Positivo" : numero< 0 ? 
"Negativo" :  "Número igual a zero"
alert (resultado)